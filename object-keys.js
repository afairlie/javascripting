const car = {
  make: "Honda",
  model: "Accord",
  year: 2020
}

const keys = Object.keys(car) // this creates a variable called 'keys' that holds an array that lists the keys (as in the key: value pair in an object) of the car object by using the Object.keys() prototype method

console.log(keys) // prints the values of the key variable